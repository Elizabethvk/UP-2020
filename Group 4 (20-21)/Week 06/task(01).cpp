#include <iostream>
#include <cmath>

int main() {
    const size_t maxSize = 300;
    int n[maxSize] = { 0 };
    int n;
    std::cin >> n;
    return 0;
}
void writeArrray(int a[], size_t size) {
    for (int i = 0; i < size; i++) {
        std::cin >> a[i];
    }
}
